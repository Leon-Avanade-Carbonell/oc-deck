'use client';

/**
 * SampleClimateTimeSlider - DEPRECATED
 *
 * This component is part of the old climate system and is being replaced by
 * SampleClimateMvtTimePicker which works with the new MVT atoms.
 *
 * Temporarily disabled to prevent interference with MVT layer rendering.
 */
export function SampleClimateTimeSlider() {
  // TODO: This component is part of the old climate system.
  // We're migrating to the new MVT atoms with dedicated components.
  // See SampleClimateMvtTimePicker for the new implementation.
  return null;
}
